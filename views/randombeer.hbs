{{#each randomArr}}

    <img src="{{image_url}}" alt="imagen">
    <h2>{{name}}</h2>
    <h4>{{description}}</h4>
    <h3>{{tagline}}</h3>
    <h4>
     {{#each food_pairing}}
        <li>
       {{this}}
        </li>
    {{/each}}
    </h4>
    <h4>{{brewers_tips}}</h4>
{{/each}}